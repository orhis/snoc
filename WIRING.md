# Wpięcie modułów — snoc

Pakiety są w `requirements.txt`. Wzorce wpięcia (zakomentowane) w `app/integrations.py`.

- **email** — odkomentuj snippet w `app/integrations.py`; szczegóły: `modules/email_smtp/README.md`
- **audit** — odkomentuj snippet w `app/integrations.py`; szczegóły: `modules/audit/README.md`
- **scheduler** — odkomentuj snippet w `app/integrations.py`; szczegóły: `modules/scheduler/README.md`

Po wpięciu uruchom `fam-doctor .` i własne testy.
